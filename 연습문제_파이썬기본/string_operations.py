# 1. 사용자로부터 문자열 입력 받기
text = input("문자열을 입력하세요: ")

# 2. 문자열 조작 및 출력
# upper(): 모두 대문자로 변환
print(f"대문자: {text.upper()}")

# lower(): 모두 소문자로 변환
print(f"소문자: {text.lower()}")

# len(): 문자열의 길이(공백 포함)를 계산
print(f"문자열 길이: {len(text)}")